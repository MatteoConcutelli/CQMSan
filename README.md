# CQMSan
A compile-time, opportunistic detector of uninitialized-memory reads (UMR) for coverage-guided fuzzing. It is an LLVM-pass adaptation of QMSan (QEMU MemorySanitizer), built on the MemorySanitizer skeleton.
